import React from 'react'

const alphabetButtons: [] = [26]

Array(25)
  .fill(null)
  .map((_, i) => i)
console.log(Array)

export function App() {
  return (
    <div>
      <h1>Do you want to build a snowman?</h1>
      <main></main>
    </div>
  )
}
